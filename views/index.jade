#fb-root

.template
 .file(draggable='true')
  .icon
   .progress
    .done
  a(draggable='false' target='_blank')
   .name

#user
 .login
  img.icon(src='/images/facebook.png')
  span.name.label Login
 .info
  img.icon
  span.name.label
  .logout Logout

#headline.label Drop files here to upload
#dropbox
#trash

#pay
 #paypal
  form(action="https://www.paypal.com/cgi-bin/webscr", method="post", target='_blank')
   input(type="hidden", name="cmd", value="_s-xclick")
   input(type="hidden", name="hosted_button_id", value="#{paypal.button.id}")
   input(type="hidden", name="on0", value="Amount")
   .select
    .option
     .value 1Gb
     .head 1Gb
     div $0.99
    .option.selected
     .value 10Gb
     .head 10Gb
     div $8.99
    .option
     .value 100Gb
     .head 100Gb
     div $69.99
   input(type='hidden', name='os0', value='1Gb')
   input(type='hidden', name='currency_code', value='USD')
   input(type='hidden', name='custom')
   img(alt="", border="0", src="https://www.paypalobjects.com/en_US/i/scr/pixel.gif", width="1", height="1")

#status
 #transfer
  .title.label Transfer
  .progress
   .done
  .available.label 1Gb
  .upgrade
   .plus.label +

